<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ricardocorrent.capptanapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".view.PautaCadastroActivity">
        </activity>
        <activity android:name=".view.PautaDetalhesActivity">
        </activity>
        <activity android:name=".view.UsuarioCadastroActivity">
        </activity>
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.RecuperarSenhaActivity">
        </activity>
        <activity android:name=".view.UsuarioDetalhesActivity">
        </activity>
        <activity android:name=".view.PautaFechadaActivity">
        </activity>
    </application>

</manifest>